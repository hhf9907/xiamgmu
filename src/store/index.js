import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)
const state = {
  sy:0
}
 const store = new Vuex.Store({
  state,
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})

export default store